<p style="color: green"><%= notice %></p>

<h1>Products</h1>

<div id="products">
  <%= @products.each do |product| %>
    <%= render product %>
    <p>
      <%= link_to "Show this product", product %>
    </p>
  <% end %>
</div>

<%= link_to "New product", new_product_path %>

</br></br>
<%= button_to "go back", dashboard_index_path, method: :get %>

